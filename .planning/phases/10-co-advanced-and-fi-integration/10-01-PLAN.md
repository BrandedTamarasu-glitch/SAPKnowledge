---
phase: 10-co-advanced-and-fi-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/co/co-advanced.md
autonomous: true

must_haves:
  truths:
    - "User can trace how primary cost elements map 1:1 to GL accounts and how this drives automatic CO document creation from FI postings"
    - "User can follow the 5-step CO-FI real-time integration flow: FI document -> cost element check (CSKB) -> CO account assignment validation (OKB9) -> CO document creation (COBK/COEP/COSP) -> PCA update (GLPCA)"
    - "User understands that CO totals ALWAYS exceed FI totals for any cost center receiving allocations — secondary CEs (21,31,41,42,43) exist only in CO and this is by design, not an error"
    - "User can follow the complete reconciliation walkthrough: compare KSB1 primary-only vs FBL3N, identify secondary-only postings, check cross-CC reconciliation (KAL1/KALC), verify PCA (KE5Z vs FAGLB03)"
    - "Reconciliation ledger (COFIT/COFIS, KALC) is documented as ECC 6-specific with S/4HANA elimination note"
    - "Cost element mapping table covers all 11 categories (1,3,4,11,12,22 primary + 21,31,41,42,43 secondary) with GL requirement, flow direction, and FI impact"
  artifacts:
    - path: "modules/co/co-advanced.md"
      provides: "Cost element mapping walkthrough, CO-FI real-time integration flow, reconciliation walkthrough, reconciliation ledger"
      min_lines: 250
      contains: "Cost Element Mapping"
    - path: "modules/co/co-advanced.md"
      provides: "YAML frontmatter with ecc_version"
      contains: "ecc_version"
    - path: "modules/co/co-advanced.md"
      provides: "5-step CO-FI real-time integration flow"
      contains: "CSKB"
    - path: "modules/co/co-advanced.md"
      provides: "Reconciliation walkthrough"
      contains: "COFIT"
  key_links:
    - from: "modules/co/co-advanced.md CE mapping"
      to: "modules/co/master-data.md"
      via: "cross-reference for cost element categories"
      pattern: "master-data.md|cost element categor"
    - from: "modules/co/co-advanced.md reconciliation"
      to: "KALC/KAL1 reconciliation ledger"
      via: "reconciliation walkthrough"
      pattern: "KALC|KAL1|COFIT"
    - from: "modules/co/co-advanced.md OKB9"
      to: "modules/co/config-spro.md"
      via: "cross-reference for OKB9 configuration"
      pattern: "OKB9|config-spro"
---

<objective>
Create modules/co/co-advanced.md with the cost element mapping walkthrough section — the CO equivalent of OBYC (MM) and VKOA (SD). Unlike MM/SD which have single account determination frameworks, CO-FI integration is bidirectional: FI->CO via primary cost elements (automatic), CO->FI only via category 22 settlement. Plan 01 writes the mapping and reconciliation content; Plan 03 will append decision trees and troubleshooting.

Purpose: Delivers CO-05 (cost element mapping and CO-FI reconciliation walkthrough). When someone asks "how do cost center postings reconcile with FI," this section traces the full path from FI posting through cost element lookup to CO document creation, and explains why CO totals are always higher than FI totals. Addresses the #1 misconception about CO-FI: that the totals should match.

Output: modules/co/co-advanced.md — first section populated (~300 lines), YAML frontmatter, four major subsections (CE mapping table, real-time integration flow, reconciliation walkthrough, reconciliation ledger).
</objective>

<execution_context>
@/home/corye/.claude/get-shit-done/workflows/execute-plan.md
@/home/corye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-co-advanced-and-fi-integration/10-RESEARCH.md
@modules/co/master-data.md
@modules/co/config-spro.md
@modules/co/processes.md
@modules/co/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create co-advanced.md with cost element mapping and CO-FI reconciliation walkthrough</name>
  <files>modules/co/co-advanced.md</files>
  <action>
Create modules/co/co-advanced.md with YAML frontmatter and the complete cost element mapping and CO-FI reconciliation walkthrough. This file will later receive decision trees and troubleshooting sections (Plan 03).

**YAML frontmatter:**
```yaml
---
module: co
content_type: decision-trees-and-troubleshooting
ecc_version: "6.0"
ehp_range: "0-8"
confidence: medium
last_verified: 2026-02-17
---
```

**File header:**
```
# Controlling — Cost Element Mapping, Decision Trees & Troubleshooting

> ECC 6.0 reference. Cost element mapping walkthrough covers how primary cost elements bridge FI and CO, how secondary cost elements live CO-only, and how to reconcile CO totals with FI. Decision trees guide CO configuration choices. Troubleshooting covers symptom-based diagnosis of common CO issues. For cost element master data and category reference, see `modules/co/master-data.md`. For the OBYC (MM) and VKOA (SD) account determination frameworks, see `modules/fi/account-determination.md`.
```

**Section 1: Cost Element Mapping and CO-FI Reconciliation**

Start with a brief intro paragraph explaining that CO-FI integration is fundamentally different from OBYC (MM) and VKOA (SD):
- OBYC maps movement types to GL accounts (one direction: MM -> FI)
- VKOA maps billing to GL accounts (one direction: SD -> FI)
- CO-FI is BIDIRECTIONAL: FI -> CO automatically via primary cost elements; CO -> FI only via category 22 settlement
- There is no single "account determination" T-code for CO — the integration is architectural, not configuration-based

Reference `modules/co/master-data.md` for the full cost element category table (do not duplicate it). State that this section adds the MAPPING walkthrough: how the categories drive CO-FI data flow.

**Subsection 1a: Cost Element Mapping Table — All 11 Categories**

Create a comprehensive mapping table covering all cost element categories with their CO-FI integration behavior. Use the data from research Section 1.3:

| Category | Name | GL Account Required | Direction of Flow | FI Impact | CO Tables Updated |
|----------|------|--------------------|--------------------|-----------|-------------------|
| 1 | Primary costs/revenue | Yes (same number) | FI -> CO (automatic) | Every FI posting to this GL account creates a CO document | COBK, COEP, COSP |
| 3 | Accrual/deferral (FI side) | Yes | FI -> CO | FI accrual posts to this GL/CE; CO may have separate imputed cost via cat 4 | COBK, COEP, COSP |
| 4 | Accrual/deferral (CO side) | Yes | CO accrual posting | CO imputed costs (calculated depreciation, calculated interest) | COBK, COEP, COSP |
| 11 | Revenue | Yes | FI -> CO | Revenue postings flow to CO (for cost-of-sales accounting / CO-PA) | COBK, COEP, COSP |
| 12 | Sales deduction | Yes | FI -> CO | Discount/rebate postings flow to CO | COBK, COEP, COSP |
| 22 | External settlement | Yes | CO -> FI | Settlement from order to GL account creates FI document | COBK, COEP, COSP + BKPF, BSEG |
| 21 | Internal settlement | No (CO-only) | CO internal | Settlement from order to cost center/order — no FI impact | COBK, COEP, COSS |
| 31 | Results analysis | No (CO-only) | CO internal | WIP and results analysis — no FI impact (unless posted to FI via RA) | COBK, COEP, COSS |
| 41 | Overhead rates | No (CO-only) | CO internal | Overhead surcharges from KGI2 — no FI impact | COBK, COEP, COSS |
| 42 | Assessment | No (CO-only) | CO internal | Assessment allocations from KSU5 — no FI impact | COBK, COEP, COSS |
| 43 | Activity allocation | No (CO-only) | CO internal | Activity allocations — no FI impact | COBK, COEP, COSS |

After the table, add the critical reconciliation insight as a blockquote:

> **CRITICAL RECONCILIATION INSIGHT:** Secondary cost elements (categories 21, 31, 41, 42, 43) exist ONLY in CO. They appear in COSP/COSS totals tables but have no corresponding GL posting. This means CO totals for a cost center will always be HIGHER than the FI GL totals for the same cost center's expense accounts, because CO includes both primary postings (from FI) and secondary allocations (CO-internal). **This is not an error — it is by design.** Only primary cost element totals should match between CO and FI.

**Subsection 1b: CO-FI Real-Time Integration Flow**

Write the complete 5-step flow from research Section 1.2, showing what happens when an FI posting hits a P&L account with a corresponding cost element:

```
Step 1: User posts FI document (FB50, FB60, MIRO, VF01, AFAB, etc.)
        Document includes a P&L GL account (e.g., 400000 Salaries)

Step 2: System checks — does a primary cost element exist with the same number?
        Lookup: CSKB where KSTAR = GL account number and KOKRS = controlling area
        If YES → CO posting is created automatically
        If NO → No CO posting (GL account is FI-only, e.g., balance sheet accounts)

Step 3: System requires a CO account assignment object
        The CO receiver must be specified: cost center, internal order, WBS element, etc.
        If OKB9 default exists for this GL account → system uses the default
        If OKB9 has no default AND user did not enter one → posting FAILS with
        "Enter a CO account assignment" (this is the most common CO error)

Step 4: CO document created
        COBK header + COEP line items
        The CO document number is DIFFERENT from the FI document number
        COEP-BELNR references the FI document for traceability
        COSP totals table updated (primary postings go to COSP)

Step 5: If the CO object (cost center, order) has a profit center assignment
        PCA separate ledger (GLPCA) is also updated
        This creates the ECC 6-specific PCA data flow
```

After the flow, add key principle as a blockquote:

> **Key principle:** Primary cost elements are the bridge between FI and CO. Every P&L posting that has a corresponding primary cost element automatically flows to CO. Balance sheet postings (asset accounts, liability accounts, equity) do NOT flow to CO because they have no cost element.

Then add the category 22 exception:

> **Exception — Category 22 (External Settlement):** When an internal order settles to a GL account (KO88 with receiver type KST), a category 22 cost element is used. This is the ONLY direction where CO creates an FI posting. All other CO-internal transactions (assessment, distribution, activity allocation) stay within CO and never create FI documents.

**Subsection 1c: CO-FI Reconciliation Walkthrough — "How Cost Center Postings Reconcile with FI"**

Write the complete 5-step reconciliation walkthrough from research Section 1.5. This directly addresses success criterion #5.

**Scenario:** User wants to verify that cost center 1000 shows the same total costs in CO reports (KSB1/S_ALR_87013611) as in FI reports (FBL3N/FAGLB03).

**Step 1: Understand What CO Sees vs What FI Sees**
- FI (FAGLB03 for the GL accounts): Shows only PRIMARY postings — actual FI document amounts posted to expense GL accounts. These are category 1/3/4/11/12 cost element postings.
- CO (KSB1 for the cost center): Shows primary postings PLUS secondary allocations (categories 21,31,41,42,43). The CO total is >= the FI total.

**Step 2: Reconcile Primary Postings**
- Run KSB1 for the cost center, filter by primary cost elements only (categories 1, 3, 4, 11, 12)
- Run FBL3N or FAGLB03 for the corresponding GL accounts in the same period
- These totals MUST match. If they don't:
  - Check if all FI postings carry a cost center assignment (OKB9 defaults, manual entry)
  - Check if some FI postings went to a different cost center (posting error)
  - Check if PCA substitution rules (1KEF) are redirecting profit center assignments

**Step 3: Identify Secondary Postings (CO-Only)**
- Run KSB1 for the cost center, filter by secondary cost elements (categories 21, 31, 41, 42, 43)
- These postings have NO FI counterpart — they exist only in CO
- They represent allocations received from other CO objects (assessment, distribution, activity allocation)
- The total of primary + secondary = full cost center costs in CO

**Step 4: Cross-Company-Code Reconciliation (If Applicable)**
- If the controlling area spans multiple company codes:
  - Run KAL1 to check the reconciliation ledger for cross-CC postings
  - Run KALC if reconciliation postings are pending
  - Verify intercompany receivables/payables in FI match the COFIT totals

**Step 5: PCA Reconciliation**
- Compare KE5Z (PCA report, reads GLPCA) totals with FAGLB03 (FI report, reads FAGLFLEXT)
- If PCA totals differ from FI: some FI postings lack profit center assignment
  - Check document splitting configuration (New GL)
  - Check 1KEF substitution rules
  - Check that every cost center has a profit center assigned (CSKS-PRCTR)

**Subsection 1d: Reconciliation Ledger (COFIT/COFIS) — ECC 6 Specific**

Write the reconciliation ledger documentation from research Section 1.4:

**What it is:** The reconciliation ledger tracks CO postings that cross company code boundaries within a single controlling area. In ECC 6, a controlling area can span multiple company codes (OKKP configuration). When CO allocates costs from a cost center in company code 1000 to a cost center in company code 2000, FI needs an intercompany entry to balance the books.

**How it works (3-step process):**

Step 1: CO posting crosses company code boundary
- Example: Assessment cycle allocates from CC 1000 (company code 1000) to CC 2000 (company code 2000)
- CO documents posted in COEP — CO is balanced

Step 2: Reconciliation ledger records the cross-CC posting
- Tables: COFIT (totals), COFIS (single records)
- Records: sending company code, receiving company code, amount, cost element

Step 3: Periodic reconciliation posting
- T-code: KALC
- Creates FI intercompany documents: Dr Intercompany Receivable (sending CC) / Cr Intercompany Payable (receiving CC)

**When reconciliation is needed:**
- Controlling area spans multiple company codes (1:many CA:CC relationship)
- CO allocations cross company code boundaries
- If all CO objects are in the same company code, reconciliation ledger postings are zero

**Key T-codes:**
- KALC — Execute reconciliation posting
- KAL1 — Display reconciliation ledger
- KAL2 — Reconciliation ledger report

> **S/4HANA Differences:** The reconciliation ledger is ELIMINATED in S/4HANA. The Universal Journal (ACDOCA) records all CO postings with full FI account assignment, so cross-company-code CO postings automatically create intercompany FI entries. KALC is no longer needed. Additionally, S/4HANA strongly recommends 1:1 controlling area to company code, further reducing cross-CC scenarios.

After writing the complete file, commit:
```
git add modules/co/co-advanced.md
git commit -m "feat(10-01): create modules/co/co-advanced.md — CE mapping & reconciliation

- Cost element mapping table: all 11 categories with GL requirement, flow direction, FI impact
- 5-step CO-FI real-time integration flow: FI doc -> CSKB lookup -> OKB9 validation -> CO doc -> PCA
- CRITICAL: CO totals always >= FI totals (secondary CEs are CO-only by design)
- CO-FI reconciliation walkthrough: primary matching, secondary identification, cross-CC, PCA
- Reconciliation ledger (COFIT/COFIS, KALC): ECC 6-specific, eliminated in S/4HANA
- Category 22 = only direction CO creates FI documents (external settlement)
- References master-data.md for CE categories (no duplication)"
```
  </action>
  <verify>
Run: wc -l modules/co/co-advanced.md
Expected: 250+ lines

Run: head -10 modules/co/co-advanced.md
Expected: YAML frontmatter starting with ---

Run: grep "COFIT\|COFIS" modules/co/co-advanced.md
Expected: Reconciliation ledger tables documented

Run: grep "CSKB" modules/co/co-advanced.md
Expected: Present in real-time integration flow

Run: grep "OKB9" modules/co/co-advanced.md
Expected: Present in real-time integration flow

Run: grep "master-data.md" modules/co/co-advanced.md
Expected: Cross-reference to the master data file

Run: grep -i "by design\|not an error" modules/co/co-advanced.md
Expected: At least one match for the critical reconciliation insight

Run: grep "KALC" modules/co/co-advanced.md
Expected: Reconciliation posting T-code documented

Run: git log --oneline -1
Expected: Commit message contains "10-01" and "co-advanced"
  </verify>
  <done>
- modules/co/co-advanced.md exists with valid YAML frontmatter (6 required fields)
- Cost element mapping table: all 11 categories (1,3,4,11,12,22 primary + 21,31,41,42,43 secondary) with GL requirement, flow direction, FI impact, and CO tables updated
- 5-step CO-FI real-time integration flow: FI document -> CSKB cost element check -> OKB9 account assignment -> COBK/COEP creation -> GLPCA PCA update
- Critical reconciliation insight: CO totals always >= FI totals because secondary CEs are CO-only (by design, not error)
- 5-step reconciliation walkthrough: primary matching, secondary identification, cross-CC (KALC), PCA (KE5Z vs FAGLB03)
- Reconciliation ledger (COFIT/COFIS): documented with 3-step process, KALC T-code, and S/4HANA elimination note
- Category 22 as only CO->FI direction documented as exception
- Cross-reference to master-data.md for CE categories (no duplication)
- File committed to git
  </done>
</task>

</tasks>

<verification>
After task completes, verify:

1. Line count: `wc -l modules/co/co-advanced.md` — expect 250+
2. Section headers: `grep "^## \|^### " modules/co/co-advanced.md` — expect Cost Element Mapping, real-time integration, reconciliation walkthrough, reconciliation ledger
3. All 11 CE categories: Content must cover categories 1,3,4,11,12,22 (primary) and 21,31,41,42,43 (secondary)
4. CO > FI totals: Content must explicitly state this is by design
5. Cross-reference: `grep "master-data" modules/co/co-advanced.md` — must reference the CO master data file
6. Frontmatter: `head -8 modules/co/co-advanced.md` — must show YAML with ecc_version: "6.0"
7. KALC: Reconciliation posting T-code must be documented with S/4HANA elimination note
</verification>

<success_criteria>
- modules/co/co-advanced.md exists, is populated (not placeholder), has valid YAML frontmatter
- Cost element mapping table covers all 11 categories with flow direction and FI impact
- 5-step CO-FI real-time integration flow documented
- CO-FI reconciliation walkthrough answers "how cost center postings reconcile with FI"
- Reconciliation ledger (COFIT/COFIS/KALC) documented as ECC 6-specific
- Critical insight: CO totals > FI totals is by design (secondary CEs)
- Cross-references master-data.md without duplicating its content
- Committed to git
</success_criteria>

<output>
After completion, create .planning/phases/10-co-advanced-and-fi-integration/10-01-SUMMARY.md using the summary template at @/home/corye/.claude/get-shit-done/templates/summary.md.

Key values for summary frontmatter:
- phase: 10-co-advanced-and-fi-integration
- plan: 01
- files_created: [modules/co/co-advanced.md]
- files_modified: []
- subsystem: co-ce-mapping-reconciliation
- affects: [10-03-decision-trees, cross-module]
</output>
