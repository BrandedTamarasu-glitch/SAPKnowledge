---
phase: 01-repository-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - ~/.claude/rules/sap-knowledge-base.md
  - ~/.claude/settings.json
autonomous: false

must_haves:
  truths:
    - "Claude can access SAP knowledge base files when working in any project directory"
    - "Global routing pointer in ~/.claude/rules/ tells Claude where to find the SAP knowledge base"
    - "additionalDirectories in settings.json gives Claude read access to SAPKnowledge repo"
  artifacts:
    - path: "~/.claude/rules/sap-knowledge-base.md"
      provides: "Global Layer 1 pointer to SAP knowledge base"
      contains: "SAPKnowledge"
    - path: "~/.claude/settings.json"
      provides: "additionalDirectories configuration for global access"
      contains: "additionalDirectories"
  key_links:
    - from: "~/.claude/rules/sap-knowledge-base.md"
      to: ".claude/rules/sap-routing.md"
      via: "Layer 1 pointer directing Claude to Layer 3 project rules"
      pattern: "SAPKnowledge"
    - from: "~/.claude/settings.json"
      to: "SAPKnowledge/"
      via: "additionalDirectories granting file access"
      pattern: "additionalDirectories"
---

<objective>
Configure Claude Code's global settings so the SAP knowledge base is accessible from any project directory, not just when working inside the SAPKnowledge repo.

Purpose: The "from any project" requirement is the most architecturally significant decision in Phase 1. Without this, the knowledge base only works when cd'd into the repo — defeating the purpose of a globally available SAP reference.

Output: ~/.claude/rules/sap-knowledge-base.md (Layer 1 pointer), updated ~/.claude/settings.json (Layer 2 access)
</objective>

<execution_context>
@/home/corye/.claude/get-shit-done/workflows/execute-plan.md
@/home/corye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-foundation/01-RESEARCH.md
@.planning/phases/01-repository-foundation/01-CONTEXT.md
@.planning/phases/01-repository-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global routing pointer and configure additionalDirectories</name>
  <files>
    ~/.claude/rules/sap-knowledge-base.md
    ~/.claude/settings.json
  </files>
  <action>
**Layer 1: Global routing pointer**

Create `~/.claude/rules/sap-knowledge-base.md` — a tiny pointer file that loads into every Claude session. This is NOT the full routing index (that lives in the project's .claude/rules/). This is just a pointer.

Content (target ~100-200 tokens):
```markdown
# SAP Knowledge Base

For SAP ECC 6.0 questions, read from ~/Claude/SAPKnowledge/

The knowledge base contains module-specific reference for MM, SD, FI, CO with
transaction codes, SPRO configuration paths, process flows, and integration points.

Start with the module's CLAUDE.md file for orientation, or read
~/Claude/SAPKnowledge/.claude/rules/sap-routing.md for the full routing index.

Important: This knowledge base covers ECC 6.0 only, not S/4HANA.
```

Keep this extremely lean — every token here is paid on EVERY Claude session across ALL projects.

**Layer 2: additionalDirectories configuration**

Read the existing `~/.claude/settings.json` first. Then merge in the additionalDirectories config WITHOUT overwriting existing settings. The file may already have content (permissions, env vars, etc.).

Add to settings.json:
- `permissions.additionalDirectories`: add `"~/Claude/SAPKnowledge"` to the array (create array if it does not exist)
- `env.CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD`: set to `"1"` (enables CLAUDE.md loading from additional directories)

IMPORTANT: Read the file first, parse as JSON, merge the new keys, write back. Do NOT overwrite existing settings. If additionalDirectories already exists as an array, append the new entry only if not already present.
  </action>
  <verify>
Verify Layer 1 pointer exists and is small:
```
cat ~/.claude/rules/sap-knowledge-base.md
wc -c ~/.claude/rules/sap-knowledge-base.md
```
Should exist and be under 800 characters (~200 tokens).

Verify Layer 2 settings:
```
cat ~/.claude/settings.json | python3 -c "import json,sys; d=json.load(sys.stdin); print('additionalDirectories:', d.get('permissions',{}).get('additionalDirectories',[])); print('CLAUDE_MD env:', d.get('env',{}).get('CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD','NOT SET'))"
```
Should show SAPKnowledge in additionalDirectories and CLAUDE_MD env set to "1".
  </verify>
  <done>
Global pointer file exists at ~/.claude/rules/sap-knowledge-base.md (under 200 tokens). ~/.claude/settings.json has additionalDirectories including SAPKnowledge path and CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify SAP knowledge base is accessible from another project</name>
  <files>none</files>
  <action>
Human verification checkpoint. Claude has automated the global configuration in Task 1. This task verifies the configuration works end-to-end by testing from a different project directory.

Present the following verification steps to the user and wait for their response.
  </action>
  <verify>
1. Open a NEW Claude Code session in a DIFFERENT project directory (not SAPKnowledge). For example: `cd ~/Claude/General && claude`
2. Ask Claude: "What SAP modules are covered in the knowledge base?"
3. Claude should be able to answer by referencing the routing index or module CLAUDE.md files from ~/Claude/SAPKnowledge/
4. Ask Claude: "Read the FI module CLAUDE.md from the SAP knowledge base"
5. Claude should successfully read ~/Claude/SAPKnowledge/modules/fi/CLAUDE.md and show its contents

If Claude cannot find or access the files:
- Check that ~/.claude/rules/sap-knowledge-base.md exists
- Check that ~/.claude/settings.json has the additionalDirectories entry
- The @import approval dialog may appear on first access — approve it
- Report what happened so we can adjust the configuration

Note: The research flagged an open question about whether child CLAUDE.md on-demand loading works from additionalDirectories. This test will answer that empirically.
  </verify>
  <done>User confirms Claude can access SAP KB files from a different project directory, or issues are identified and documented for resolution.</done>
</task>

</tasks>

<verification>
1. ~/.claude/rules/sap-knowledge-base.md exists and is under 200 tokens
2. ~/.claude/settings.json includes SAPKnowledge in additionalDirectories
3. CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD env var is set to "1"
4. Human verified: Claude can access SAP KB from a different project directory
</verification>

<success_criteria>
- Global pointer file is minimal (~100-200 tokens) and correctly references the knowledge base location
- settings.json properly configured without breaking existing settings
- Claude can access SAP knowledge base files from any project directory (human verified)
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-foundation/01-03-SUMMARY.md`
</output>
