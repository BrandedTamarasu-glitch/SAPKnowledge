---
phase: 01-repository-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/CLAUDE.md
  - .claude/rules/sap-routing.md
  - .claude/rules/sap-disambiguation.md
  - .claude/rules/sap-org-structure.md
autonomous: true

must_haves:
  truths:
    - "Repository has complete directory structure with modules/, cross-module/, reference/, .claude/rules/"
    - "Routing index in .claude/rules/ tells Claude which module files to read based on query context"
    - "Root CLAUDE.md explains purpose and navigation for anyone entering the repo"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project root CLAUDE.md with purpose and usage"
      contains: "ECC 6"
    - path: ".claude/CLAUDE.md"
      provides: "Claude Code behavior instructions for this repo"
      contains: "routing"
    - path: ".claude/rules/sap-routing.md"
      provides: "Query-to-file routing table for SAP questions"
      contains: "Module Routing"
    - path: ".claude/rules/sap-disambiguation.md"
      provides: "ECC 6 vs S/4HANA quick reference"
      contains: "S/4HANA"
    - path: ".claude/rules/sap-org-structure.md"
      provides: "Org unit hierarchy summary"
      contains: "company code"
  key_links:
    - from: ".claude/rules/sap-routing.md"
      to: "modules/*/CLAUDE.md"
      via: "routing table directing Claude to module directories"
      pattern: "modules/(fi|mm|sd|co)/"
---

<objective>
Create the directory structure, root CLAUDE.md files, and .claude/rules/ routing content for the SAP ECC 6 Knowledge Base.

Purpose: Establishes the scaffolding directories and always-loaded routing rules that all subsequent plans build upon. Plan 01-02 will populate the template content files into these directories.

Output: Directory tree (empty module/cross-module/reference dirs), 5 files with real content (root CLAUDE.md, .claude/CLAUDE.md, 3 rules files).
</objective>

<execution_context>
@/home/corye/.claude/get-shit-done/workflows/execute-plan.md
@/home/corye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-foundation/01-RESEARCH.md
@.planning/phases/01-repository-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure and root CLAUDE.md files</name>
  <files>
    CLAUDE.md
    .claude/CLAUDE.md
  </files>
  <action>
Create the full directory tree for the knowledge base:
```
mkdir -p modules/{fi,mm,sd,co} cross-module reference scripts .githooks .claude/rules
```

Create `CLAUDE.md` (project root) — purpose statement explaining this is an SAP ECC 6.0 knowledge base for Claude Code context loading. Include: what this repo is, how to use it (navigate to module directories, read CLAUDE.md for orientation), that it covers ECC 6 only (not S/4HANA). Keep under 400 tokens.

Create `.claude/CLAUDE.md` — instructions for Claude Code behavior when working within this repo. Include: how to navigate the KB (start with routing index, then module CLAUDE.md, then specific files), always check frontmatter confidence level, when content mentions S/4 it is for disambiguation only. Keep under 300 tokens.
  </action>
  <verify>
Verify directory structure exists:
```
ls -R modules/ cross-module/ reference/ scripts/ .githooks/ .claude/
```
Verify both files exist and have content:
```
wc -c CLAUDE.md .claude/CLAUDE.md
```
  </verify>
  <done>
Directory tree matches the structure from the research. Two CLAUDE.md files created with real content (not placeholders).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .claude/rules/ routing, disambiguation, and org structure files</name>
  <files>
    .claude/rules/sap-routing.md
    .claude/rules/sap-disambiguation.md
    .claude/rules/sap-org-structure.md
  </files>
  <action>
Create `.claude/rules/sap-routing.md` — the always-loaded routing index. Use the structure from the research (Section 6). Include:
- No `paths:` frontmatter (loads unconditionally)
- Module routing table: topic keywords -> module -> key files
- Topics: procurement/purchasing/POs/vendors -> MM, inventory/goods movements/MIGO -> MM, sales orders/deliveries/billing -> SD, GL/AP/AR/assets -> FI, cost centers/internal orders/profit centers -> CO, account determination/OBYC/VKOA -> cross-module integration files, E2E processes -> cross-module/, movement types/doc types/posting keys -> reference/
- "How to Use" instructions (identify module, navigate, read CLAUDE.md, read specific files)
- ECC 6 only reminder
- Target: 300-500 tokens

CRITICAL: The routing table MUST reference `modules/{module}/CLAUDE.md` as the entry point for each module (not individual content files directly). This enables on-demand loading via the CLAUDE.md index pattern.

Create `.claude/rules/sap-disambiguation.md` — ECC 6 vs S/4HANA quick reference. Include a table of the top 10-15 key differences:
- Vendor/customer master (XK01/FK01/MK01) vs Business Partner (BP)
- Separate material documents (MKPF/MSEG) vs MATDOC
- Optional Material Ledger vs mandatory
- Classic GL vs Universal Journal (ACDOCA)
- Classic reporting vs Embedded Analytics
- Document splitting optional vs mandatory
- GUI transactions vs Fiori apps
- Target: 400-600 tokens

Create `.claude/rules/sap-org-structure.md` — org unit hierarchy summary. Include:
- Client -> Company Code -> Plant -> Storage Location
- Company Code -> Controlling Area -> Cost Center/Profit Center
- Sales Org -> Distribution Channel -> Division (Sales Area)
- Purchasing Org -> Purchasing Group
- Key relationships: which org units are assigned to which
- Target: 200-400 tokens

CRITICAL: All three .claude/rules/ files combined must stay under 1500 tokens. Use character count / 4 to estimate. Be concise — these load into EVERY Claude session.
  </action>
  <verify>
Verify all 3 rules files exist and have content:
```
wc -c .claude/rules/sap-routing.md .claude/rules/sap-disambiguation.md .claude/rules/sap-org-structure.md
```
Verify combined .claude/rules/ token budget — total characters across all 3 rules files divided by 4 should be under 1500.

Verify routing table references module CLAUDE.md entry points:
```
grep -c "modules/fi/" .claude/rules/sap-routing.md
grep -c "modules/mm/" .claude/rules/sap-routing.md
grep -c "modules/sd/" .claude/rules/sap-routing.md
grep -c "modules/co/" .claude/rules/sap-routing.md
```
Each should return at least 1 match.

Verify routing table references cross-module/ and reference/ directories:
```
grep -c "cross-module/" .claude/rules/sap-routing.md
grep -c "reference/" .claude/rules/sap-routing.md
```
Each should return at least 1 match.
  </verify>
  <done>
Three .claude/rules/ files created with real content. Combined token estimate under 1500. Routing table correctly maps query topics to module directories with CLAUDE.md as entry points. Cross-module and reference directories are referenced.
  </done>
</task>

</tasks>

<verification>
1. Directory tree matches research Section 2 structure exactly
2. `ls -R` shows all expected directories
3. Root CLAUDE.md and .claude/CLAUDE.md have real content
4. All .claude/rules/ files combined estimate under 1500 tokens
5. Routing table maps topics to correct module paths
6. Routing table references modules/*/CLAUDE.md as entry points (not individual content files)
7. Routing table references cross-module/ and reference/ directories
</verification>

<success_criteria>
- 5 files created (root CLAUDE.md, .claude/CLAUDE.md, 3 rules files)
- Directory tree has all expected directories (modules/fi,mm,sd,co, cross-module, reference, scripts, .githooks, .claude/rules)
- .claude/rules/ routing index maps query topics to module directories
- .claude/rules/ combined token budget under 1500
- Routing table uses modules/*/CLAUDE.md as entry points for on-demand loading
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-foundation/01-01-SUMMARY.md`
</output>
