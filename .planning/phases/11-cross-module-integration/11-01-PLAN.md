---
phase: 11-cross-module-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cross-module/mm-sd-integration.md
autonomous: true
requirements:
  - INTG-04

must_haves:
  truths:
    - "Reader can trace how SD availability check triggers MM stock lookup"
    - "Reader can follow returns from SD return order through GR back to stock and credit memo"
    - "Reader can understand PGI document flow from SD delivery through MM inventory and FI posting"
    - "Reader can find consignment, STO, third-party, and subcontracting MM-SD flows"
  artifacts:
    - path: "cross-module/mm-sd-integration.md"
      provides: "MM-SD integration point documentation"
      contains: "Availability Check"
  key_links:
    - from: "cross-module/mm-sd-integration.md"
      to: "modules/sd/config-spro.md"
      via: "cross-reference pointer"
      pattern: "See.*modules/sd/config-spro\\.md"
    - from: "cross-module/mm-sd-integration.md"
      to: "modules/mm/integration.md"
      via: "cross-reference pointer"
      pattern: "See.*modules/mm/integration\\.md"
---

<objective>
Create the MM-SD integration reference documenting how Materials Management and Sales & Distribution interact -- availability check (ATP), goods issue for delivery (PGI), returns, consignment, stock transport orders, third-party processing, and subcontracting.

Purpose: INTG-04 requires MM-SD integration point documentation. This is the only cross-module integration pair not yet documented (MM-FI, SD-FI, CO-FI all completed in phases 6, 8, 10).
Output: cross-module/mm-sd-integration.md
</objective>

<execution_context>
@/home/corye/.claude/get-shit-done/workflows/execute-plan.md
@/home/corye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cross-module-integration/11-CONTEXT.md
@.planning/phases/11-cross-module-integration/11-RESEARCH.md

# Source content for cross-references
@modules/sd/config-spro.md
@modules/sd/processes.md
@modules/sd/integration.md
@modules/mm/integration.md
@modules/mm/processes.md
@reference/movement-types.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MM-SD integration core sections (ATP, PGI, Returns)</name>
  <files>cross-module/mm-sd-integration.md</files>
  <action>
Create new file cross-module/mm-sd-integration.md with frontmatter (module: cross-module, content_type: integration, ecc_version: "6.0", ehp_range: "0-8", confidence: high, last_verified: 2026-02-17, related_modules: [mm, sd]).

Structure with 7 top-level ## sections per CONTEXT.md locked decisions. The first 3 are core:

1. **## Availability Check (ATP)** -- Core section.
   - Check types: stock check only (01), planned receipts (02), delivery scheduling
   - How SD triggers: VA01 schedule line determination reads checking group (material master MRP 3 view) + checking rule (per SD transaction type via OVZ2)
   - What MM provides: unrestricted stock from MARD/MARC, MRP planned receipts from MDBS/MDPS
   - Include enough integration mechanics to explain the SD-to-MM handshake, but point to `modules/sd/config-spro.md` Section Availability Check for SPRO-level OVZ2 config detail
   - Use inline brief + pointer format per locked decision: one-line summary + `See modules/xx/file.md Section X for full detail.`

2. **## Goods Issue for Delivery (PGI)** -- Core section.
   - VL02N posts goods issue with movement type 601
   - MM consequence: updates MARD unrestricted stock, creates material document (MKPF/MSEG)
   - FI consequence: OBYC GBB/BSX posts COGS debit + inventory credit
   - Document links: Delivery note -> material document -> FI accounting document
   - Point to `modules/mm/integration.md` Section MIGO for OBYC walkthrough, `modules/sd/integration.md` Section PGI COGS trace

3. **## Returns** -- Core section, full reverse trace per locked decision.
   - SD return order (VA01 with order type RE) -> return delivery (VL01N) -> GR back to stock (movement type 651 via MIGO or automatic) -> credit memo request -> credit memo (VF01)
   - Each step shows: (a) which module owns the step, (b) what document is created, (c) what crosses module boundary
   - Point to `modules/sd/processes.md` Section Returns for SD-side detail

Add a Prerequisites section at the top listing module files to read for full detail (same pattern as E2E files per locked decision).

Do NOT re-document content that exists in module files. Each integration point gets a brief inline summary + pointer. If writing more than 3-4 lines about a single-module step, it belongs in the module file.
  </action>
  <verify>
Run: python3 scripts/validate.py cross-module/mm-sd-integration.md
Verify file has frontmatter, ## Availability Check, ## Goods Issue for Delivery, ## Returns sections.
Verify cross-reference pointers use consistent format: See `modules/xx/file.md` Section X for full detail.
  </verify>
  <done>File exists with 3 core MM-SD integration sections (ATP, PGI, Returns), each with integration handoff detail and cross-reference pointers to existing module content.</done>
</task>

<task type="auto">
  <name>Task 2: Add extended MM-SD sections (Consignment, STO, Third-Party, Subcontracting) and S/4 differences</name>
  <files>cross-module/mm-sd-integration.md</files>
  <action>
Append 4 extended sections plus S/4HANA differences to cross-module/mm-sd-integration.md:

4. **## Consignment** -- Extended section (own ## heading per Claude's discretion -- distinct movement types justify separate section).
   - Fill: movement type 631 (consignment stock to customer, no revenue)
   - Issue: movement type 633 (customer consumes, triggers billing -- revenue recognition point)
   - Returns: movement type 634 (customer returns consignment stock)
   - Pickup: movement type 632 (company retrieves unsold consignment)
   - Key point: customer consignment stock tracked at SLOC level in MM (special stock indicator W)
   - Billing only on 633 issue -- this is the critical integration handoff

5. **## Stock Transport Orders (STO)** -- Extended section (own ## heading).
   - PO type UB, movement types 641 (GI at sending plant) / 101 (GR at receiving plant)
   - Two variants: one-step (direct) vs two-step (with in-transit)
   - Cross-plant, possibly cross-company-code (triggers intercompany billing)
   - Delivery-based STO: SD delivery document VL10B created for shipping leg
   - Brief, with pointer to modules/mm/processes.md for procurement side

6. **## Third-Party Processing** -- Extended section (can be briefer).
   - SD order with item category TAS -> automatic PR in MM -> PO to external vendor
   - Vendor ships directly to customer; no goods movement through own warehouse
   - MIRO for vendor invoice; VF01 for customer billing
   - Key: no inventory posting, only financial postings

7. **## Subcontracting (SD triggers MM)** -- Extended section (brief).
   - SD order creates demand; MRP generates subcontracting PR
   - Movement type 541 (components out to subcontractor), 101 (finished goods receipt)
   - Brief with pointer to modules/mm/processes.md for procurement-side detail

8. **## S/4HANA Differences** -- Table format consistent with all other KB files.
   - ATP: Advanced Available-to-Promise (aATP) replaces basic ATP
   - Material documents: MATDOC replaces MKPF/MSEG
   - Consignment: Same movement types, different underlying tables
   - STO: Delivery-based STO enhanced with Advanced Shipping

Run validation after completing the file.
  </action>
  <verify>
Run: python3 scripts/validate.py cross-module/mm-sd-integration.md
Verify file has all 7 integration sections plus S/4HANA Differences.
Verify movement types are correct: 601 (PGI), 651 (returns GR), 631/632/633/634 (consignment), 641/101 (STO), 541/101 (subcontracting).
  </verify>
  <done>Complete mm-sd-integration.md with 7 integration sections (3 core + 4 extended) plus S/4HANA differences. All sections use inline brief + pointer cross-reference format. No content duplication with existing module files.</done>
</task>

</tasks>

<verification>
- cross-module/mm-sd-integration.md exists with valid frontmatter
- All 7 integration areas documented: ATP, PGI, Returns, Consignment, STO, Third-Party, Subcontracting
- Cross-references use consistent format throughout
- No substantial content duplication with modules/mm/ or modules/sd/ files
- S/4HANA differences section present
- python3 scripts/validate.py passes
</verification>

<success_criteria>
- MM-SD integration file covers all locked decision requirements (core three + extended four)
- ATP section explains check types and SD trigger mechanism
- Returns section provides full reverse trace with every document and module handoff
- Each section clearly identifies what SD triggers, what MM does, what documents are created, and what FI postings result
</success_criteria>

<output>
After completion, create `.planning/phases/11-cross-module-integration/11-01-SUMMARY.md`
</output>
